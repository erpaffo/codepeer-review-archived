<h1><%= @project.name %></h1>
<p><%= @project.description %></p>

<h2>Files</h2>
<ul>
  <% @project.files.each do |file| %>
    <li><%= link_to file.filename.to_s, url_for(file) %></li>
  <% end %>
</ul>
