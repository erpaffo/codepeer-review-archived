<h1>Your GitHub Repositories</h1>

<% if @repositories.present? %>
  <ul>
    <% @repositories.each do |repo| %>
      <li>
        <%= link_to repo.full_name, repository_path(repo.full_name) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No repositories found.</p>
<% end %>
